baseURL: "https://uxui.cat"

title: UX/UI Barcelona

sectionPagesMenu: main

params:
  title: UX/UI Barcelona
  description: Learn User Experience and Interaction Design in Barcelona

taxonomies:
  topics: topics
  author: authors
  source: sources

related:
  threshold: 80
  includeNewer: false
  toLower: false
  indices:
    - name: topics
      weight: 100
    - name: authors
      weight: 80
    - name: sources
      weight: 20
    - name: date
      weight: 10


markup:
  goldmark:
    renderer:
      unsafe: true

enableGitInfo: true

frontmatter:
  date: ["date", "publishDate", "lastmod", ":fileModTime", ":git"]
  lastmod: ["lastmod", ":fileModTime", ":default"]
